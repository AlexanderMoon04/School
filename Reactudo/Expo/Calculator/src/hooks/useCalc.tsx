






export const useCalc = () => {
   const [formula, setFormula] = useState("0");
   const [currentNumber, setCurrentNumber] = useState("0");
   const [lastCalc, useRef<LastCalcData>({lastResult: 0});
   )
}